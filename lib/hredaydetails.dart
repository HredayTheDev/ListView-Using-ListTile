import 'package:flutter/material.dart';

class HredayDetails extends StatelessWidget {
  const HredayDetails({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("This Is Appbar"),),
      
    );
  }
}